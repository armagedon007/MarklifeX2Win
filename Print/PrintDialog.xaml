<Window x:Class="MarklifeWin.Print.PrintDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Marklife X2 — Print"
        Width="320" Height="280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Topmost="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Marklife X2" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <!-- Paper size -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Label size:" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" x:Name="PaperSizeCombo" SelectedIndex="0">
                <ComboBoxItem Tag="40x30">40 × 30 mm</ComboBoxItem>
                <ComboBoxItem Tag="50x30">50 × 30 mm</ComboBoxItem>
                <ComboBoxItem Tag="60x40">60 × 40 mm</ComboBoxItem>
                <ComboBoxItem Tag="80x60">80 × 60 mm</ComboBoxItem>
                <ComboBoxItem Tag="100x80">100 × 80 mm</ComboBoxItem>
            </ComboBox>
        </Grid>

        <!-- Copies -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Copies:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="CopiesBox" Text="1"
                     TextAlignment="Center" VerticalContentAlignment="Center"
                     PreviewTextInput="CopiesBox_PreviewTextInput"/>
        </Grid>

        <!-- Job info -->
        <TextBlock Grid.Row="3" x:Name="JobInfoText"
                   Text="" FontSize="10" Foreground="Gray" Margin="0,0,0,8"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="Print" Width="80" IsDefault="True"
                    Background="#2196F3" Foreground="White" BorderThickness="0"
                    Click="Print_Click"/>
        </StackPanel>
    </Grid>
</Window>
